# Offline end-to-end fixture: real SDT test split, echo provider, all five arms.
name: tcm-test-sdt
dataset:
  kind: sdt
  path: data/sdt/Test_TCM_Data_v1.json
  limit: 4
task: sdt
domain: clinical_reasoning
models: [echo]
conditions: [M0, M1, M2, M3, M4]
samples: 1
decode: {temperature: 0.0, max_tokens: 512, seed: 7}
retrieval: {top_k: 4, graph_hops: 1}
tool_budget: {max_calls: 4, max_calls_per_tool: 2, max_result_chars: 1500}
context_budget: {max_chars: 3000, max_entities_per_block: 4}
max_agent_turns: 6
output_dir: runs/test
concurrency: 1
